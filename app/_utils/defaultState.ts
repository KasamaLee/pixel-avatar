import type { AvatarState } from './types';

export const DEFAULT_AVATAR_STATE: AvatarState = {
  activeCategory: 'head',
  selections: {
    head: 'longstraight',
    hijab: 'none',
    body: 'f5',
    prop: 'none',
  },
  showBackground: true,
};
